---
component_roots:
  - bootstrap

inputs: &inputs-defaults
  environments:
    root: "649115683993"

bootstrap: &bootstrap-defaults
  resource_groups: {}

root:
  inputs:
    <<: *inputs-defaults
  bootstrap:
    <<: *bootstrap-defaults
